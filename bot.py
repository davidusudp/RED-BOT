import random
import threading
import socket
ip = input("IP: ");
port = input("Port: ");
choice = input("Choice: ");
bots = input("Bots: ");
nickname = "MCBOT31"

for yes in choice():
th.Threading(target=test);
thread.start()

for thread in threads():
thread.join();
